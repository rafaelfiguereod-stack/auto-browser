# CUAVerifierBench

Placeholder for the Stage 1 verifier regression suite.

Target dependency: `microsoft/fara` CUAVerifierBench, pinned to a reviewed commit before CI enforcement. The harness verifier adapter boundary is already in `controller/app/harness/verifier/uv.py`; this directory is reserved for the pinned benchmark fixture or submodule wiring.
